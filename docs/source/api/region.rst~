region API
==========

The region API provides information about any regions that match the supplied parameters. When calling the API externally, you do not need to provide a `session` parameter; it will be automatically supplied by the backend.

.. autofunction:: scip.api.region
